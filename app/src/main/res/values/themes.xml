<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Тёмная тема окна: экран запуска не должен мигать белым до отрисовки Compose. -->
    <style name="Theme.MyApplication" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/app_background_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
